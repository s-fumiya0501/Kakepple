# ========================================
# 本番環境用 環境変数テンプレート
# このファイルを .env.production にコピーして値を設定
# ========================================

# ----------------------------------------
# Database Configuration
# ----------------------------------------
POSTGRES_USER=kakepple_prod
POSTGRES_PASSWORD=<強力なパスワードを設定>
POSTGRES_DB=kakepple_prod

# ----------------------------------------
# Redis Configuration
# ----------------------------------------
REDIS_PASSWORD=<強力なパスワードを設定>

# ----------------------------------------
# Security
# ----------------------------------------
# 32文字以上のランダムな文字列を設定
# 生成コマンド: openssl rand -hex 32
SECRET_KEY=<ランダムな秘密鍵>

# ----------------------------------------
# Google OAuth 2.0
# https://console.cloud.google.com/apis/credentials
# ----------------------------------------
GOOGLE_CLIENT_ID=<Google OAuth クライアントID>
GOOGLE_CLIENT_SECRET=<Google OAuth クライアントシークレット>
GOOGLE_REDIRECT_URI=https://api.yourdomain.com/api/auth/google/callback

# ----------------------------------------
# LINE OAuth (任意)
# https://developers.line.biz/console/
# ----------------------------------------
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=

# ----------------------------------------
# URLs
# ----------------------------------------
# フロントエンドのURL (CORS設定に使用)
FRONTEND_URL=https://yourdomain.com

# バックエンドのURL (フロントエンドからのAPI呼び出しに使用)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ----------------------------------------
# Optional: SMTP (メール送信用)
# ----------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com
